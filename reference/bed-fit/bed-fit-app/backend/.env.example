# Copy to .env and fill in. Only the key is required, and only for reading
# uploaded plans — /api/sample and the placement engine work without it.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
BEDFIT_MODEL=claude-sonnet-5
BEDFIT_CORS=http://localhost:5173
